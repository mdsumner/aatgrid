% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bounds_materialize.R
\name{tiles_for_extent2}
\alias{tiles_for_extent2}
\title{Tiles covering a lonlat extent, as a data.frame with wk geometry}
\usage{
tiles_for_extent2(
  extent_lonlat,
  res,
  zone = NULL,
  zones = define_utm_zones(),
  pad = 0,
  n = 21
)
}
\arguments{
\item{extent_lonlat}{c(xmin, xmax, ymin, ymax), degrees}

\item{res}{Numeric resolution in metres, or a legacy level name}

\item{zone}{Zone id (e.g. "43S"); default: centroid zone}

\item{zones}{Zone table (see define_utm_zones())}

\item{pad}{Integer collar of extra tile rings (default 0). Padding is
tile-denominated on purpose: "one ring of context ocean" is a
statement about the grid; a padded lonlat bbox is not.}

\item{n}{Boundary densification, points per edge}
}
\value{
data.frame: tile_id, zone_id, res, col, row, geometry
(wk::rct with the zone CRS attached)
}
\description{
Zone policy: one zone per call. By default the zone containing the
extent's centroid longitude is used; regions straddling a zone
boundary are a registry decision, not an arithmetic one -- pass
\code{zone} explicitly to override, and call twice for a deliberate
two-zone scheme (ids are zone-qualified, so they cannot collide).
}
